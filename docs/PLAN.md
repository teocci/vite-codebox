# Active Plan

No active plan.

The last plan (R4 — agent-driven viewer control, P-15 through P-19) shipped as v0.9.0 on 2026-07-28
and was drained. `integration = trunk`, so there was no branch or worktree to prune.

Scaffold the next one with `dev-phase-start` once a plan is approved; it refuses to append to a
fully-released ledger, which is why this file is reduced to the stub rather than left as a record.
The record lives in `docs/RELEASE.md` and `docs/PROGRESS.md`.
