# I-14 — A view verb group, and a real bool field type in the contract

- **Improvement ID:** I-14
- **Version:** 0.9.0
- **Date:** 2026-07-28
- **Status:** ✅ DONE (542 tests; live-verified).
- **Related work:** depends on the ops and the bool type published by the first phase

## Objective

Presentation is not a build verb and does not belong in an exec batch — exec is the batch runner. Give it its own group the way auth has one. Implement bool in the Go client rather than deferring it as axis is deferred: axis defers because its value domain is server data, while bool is a structural JSON check, and deferring it means a bad flag lands all the parts in the batch and silently skips the rotate.

## Approach

A new `dispatch_view.go` holds the group, following `dispatchAuth`/`dispatchBuild`: `codeblox view
<1-6>` (a numeric argument is a preset, a word is a subcommand), `view reframe`, and
`view rotate|grid|hud on|off`. It reuses `flagSurface`, the existing session path, `--json` and
`--dry-run`, and rejects unknown subcommands and stray arguments the way I-1 established.
`dispatch.go` gains the routing arm and the usage text. The filename suffix `view` collides with no
`GOOS`/`GOARCH`, so it carries no implicit build constraint.

The group exists because presentation is not a build verb. `exec` is the batch runner — it parses a
JSON array, object or NDJSON and validates the whole batch before sending — and routing camera and
HUD direction through it is a category error. The CLI's own convention already says so: `clear` is
an op *and* a verb, as are `box`, `sphere`, `cylinder` and `remove`.

`contract.go` gets `typeBool` and a `checkField` case, about six lines. This is deliberately **not**
the `axis` deferral: `axis` defers because its value domain (`x|y|z`) is server data the client
refuses to compile in, whereas `bool` is a structural JSON check fully described by its type name,
in the same category as `int+` and `id` — both of which are implemented. The reason it matters is
failure behaviour, not tidiness: `applyBatch` records a rejected command in `errors` and continues,
so `[...30 parts..., {op:'rotate',on:'yes'}]` would land all thirty parts and silently not rotate,
with the error buried in an ack the CLI drops. Client-side rejection kills the batch before anything
is sent, exit 5.

`view.n` stays `int+` on the Go side — a `'view'` type would compile in "there are six presets",
precisely the server knowledge this package refuses to hold. A test asserts `n: 7` produces **no**
client-side error, so nobody later "fixes" it by hardcoding the count; the server refuses it.

## Files changed

| File | Change |
|---|---|
| `clients/codeblox/internal/command/dispatch_view.go` | New. `dispatchView` plus `viewCommand` (a numeric argument is a preset, a word is a subcommand) and `parseOnOff`. Reuses `flagSurface`, `registerCommon`, `registerDryRun`, the session path and `RunOne`. |
| `clients/codeblox/internal/command/dispatch.go` | `case "view"` routing arm; a `presentation:` block in the usage text. |
| `clients/codeblox/internal/contract/contract.go` | `typeBool` const and its `checkField` case, with the axis-vs-bool distinction recorded where the decision lives. |
| `clients/codeblox/internal/command/dispatch_view_test.go` | New. 12 test functions over the real `Dispatch` path with a fake session. |
| `clients/codeblox/internal/contract/contract_test.go` | `rotate` and `tube` added to the sample contract; 3 new tests (bool accepted, non-bool rejected, axis still deferred); the fixture's op count updated 4 → 6. |

## Verification

`npm run test:cli` — 127 test functions, 15 new, run with `-count=1` so no result came from cache.
`npm run test:e2e` — 24, green against the live server. `go vet ./...` clean.

The argv shape needed one adjustment the approach did not anticipate: `flagSurface.parse` rejects
positionals outright ("No verb takes positionals — batches arrive on stdin"), so `view` cannot hand
its whole argv to the flag set. `viewCommand` consumes the leading tokens and returns the remainder,
which is what `parse` then sees — the same split `dispatchAuth` already does with its subcommand.

Writing the usage text also hit a real trap: `usage` is a raw-string literal, so the backticks the
rest of the docs use around `codeblox info` terminated the string and broke the build. They are gone
from that block.

**End-to-end against the running server**, using the built binary:

```
view 1                 {"ok":true,"sent":1,...}                                    exit 0
view rotate off        {"ok":true,"sent":1,...}                                    exit 0
view 99                errors:["n must be an integer 1..6"]                        exit 6
view grid yes          view grid: want on or off, got "yes"                        exit 2
view zoom              unknown view subcommand "zoom": want a preset number, ...   exit 2
info                   view n:int+   reframe (no fields)   rotate on:bool  ...
```

`view 99` reaching exit **6** and not exit 5 is the design working: the client forwards it because
`n` is `int+`, and the server — the only party that knows there are six presets — refuses it.

The `bool` case was demonstrated on the failure it exists to prevent, not just asserted:

```
[{"op":"box",...},{"op":"rotate","on":"yes"}]
  -> {"ok":false,"code":"contract_rejected","exit":5,
      "detail":"command 1: rotate: on must be true or false, got yes"}
```

Exit 5, nothing sent — the box did not land. Before this change that batch would have placed the box
and silently not rotated, with the reason in an ack the CLI drops.
